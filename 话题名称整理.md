一楼巡逻
  ↓
识别并融合所有箱子数字
  ↓
统计 digit 数字出现次数
  ↓
发布 /least_frequent_digit 最小的数据 rostopic echo /least_frequent_digit
  ↓
mission_manager 保存 target_digit
  ↓
去二楼
  ↓
依次到 door_obs_1, door_obs_2, door_obs_3, door_obs_4
  ↓
每到一个观察点，读取 /current_seen_digit 目前看到的数据rostopic echo /current_seen_digit
  ↓
若 current_seen_digit == target_digit 比较看到的和最小的
  ↓
当前 door_id 即 target_door_id      rostopic echo /rosout | grep TARGET_DOOR
  ↓
导航到 door_entry_goals[target_door_id]      看目标点：rostopic echo /move_base_simple/goal